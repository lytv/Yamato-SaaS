/**
 * EmployeeSalaryEntry Enhanced Validation Schemas with Complex Rules
 * Generated by enhanced entity generator script V4
 * 🆕 Features: Text FK Validation, Decimal Validation, Timestamp Validation
 */

import { z } from 'zod';

// 🆕 V4: Enhanced form validation schema with complex rules
export const employeeSalaryEntryFormObject = z.object({
  userId: z.string().optional(),
  productionStepDetailId: z.number().int().positive().optional(),
  planId: z.number().int().positive().optional(),
  workDate: z.union([z.string(), z.date()]).refine((date) => {
    const d = new Date(date);
    const now = new Date();
    const threeMonthsAgo = new Date(now.getFullYear(), now.getMonth() - 3, now.getDate());
    return d >= threeMonthsAgo && d <= now;
  }, 'Work date must be within the last 3 months'),
  entryDate: z.union([z.string(), z.date()]).optional(),
  actualQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  plannedQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  limitQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  previousEnteredQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  unitPrice: z.number().min(0, 'Value must be non-negative').max(99999999.99, 'Unit Price exceeds maximum allowed value').optional(),
  totalAmount: z.number().min(0, 'Value must be non-negative').max(9999999999.99, 'Total Amount exceeds maximum allowed value').optional(),
  salaryNote: z.string().trim().optional(),
  status: z.string().trim().regex(/^(draft|submitted|approved|paid|cancelled|)$/, 'Invalid status value').optional(),
  approvedBy: z.string().trim().optional(),
  approvedAt: z.union([z.string(), z.date()]).optional(),
  startTime: z.union([z.string(), z.date()]).refine((date) => {
    if (!date) {
      return true;
    }
    const d = new Date(date);
    const now = new Date();
    const oneDayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1000);
    return d >= oneDayAgo && d <= now;
  }, 'Start Time must be within the last 24 hours').optional(),
  endTime: z.union([z.string(), z.date()]).refine((date) => {
    if (!date) {
      return true;
    }
    const d = new Date(date);
    const now = new Date();
    const oneDayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1000);
    return d >= oneDayAgo && d <= now;
  }, 'End Time must be within the last 24 hours').optional(),
  workDurationMinutes: z.number().int().min(0).max(1440, 'Work duration cannot exceed 24 hours').optional(),
});

export const employeeSalaryEntryFormSchema = employeeSalaryEntryFormObject.refine((data) => {
  if (data.actualQuantity && data.unitPrice && data.totalAmount) {
    const calculated = data.actualQuantity * data.unitPrice;
    return Math.abs(calculated - data.totalAmount) < 0.01;
  }
  return true;
}, {
  message: 'Total amount must equal actual quantity × unit price',
  path: ['totalAmount'],
});

// Create employeeSalaryEntry schema (same as form + ownerId + required fields validation)
export const createEmployeeSalaryEntrySchema = z.object({
  ownerId: z.string().min(1, 'Owner ID is required'),
  userId: z.string().min(1, 'Employee is required'),
  productionStepDetailId: z.number().int().positive('Production Step Detail is required'),
  planId: z.number().int().positive('Plan is required'),
  workDate: z.union([z.string(), z.date()]).refine((date) => {
    const d = new Date(date);
    const now = new Date();
    const threeMonthsAgo = new Date(now.getFullYear(), now.getMonth() - 3, now.getDate());
    return d >= threeMonthsAgo && d <= now;
  }, 'Work date must be within the last 3 months'),
  entryDate: z.union([z.string(), z.date()]).optional(),
  actualQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  plannedQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  limitQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  previousEnteredQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  unitPrice: z.number().min(0, 'Value must be non-negative').max(99999999.99, 'Unit Price exceeds maximum allowed value').optional(),
  totalAmount: z.number().min(0, 'Value must be non-negative').max(9999999999.99, 'Total Amount exceeds maximum allowed value').optional(),
  salaryNote: z.string().trim().optional(),
  status: z.string().trim().regex(/^(draft|submitted|approved|paid|cancelled|)$/, 'Invalid status value').optional(),
  approvedBy: z.string().trim().optional(),
  approvedAt: z.union([z.string(), z.date()]).optional(),
  startTime: z.union([z.string(), z.date()]).optional(),
  endTime: z.union([z.string(), z.date()]).optional(),
  workDurationMinutes: z.number().int().min(0).max(1440, 'Work duration cannot exceed 24 hours').optional(),
});

// Update employeeSalaryEntry schema (all fields optional)
export const updateEmployeeSalaryEntrySchema = z.object({
  userId: z.string().min(1, 'Employee is required').optional(),
  productionStepDetailId: z.number().int().positive('Production Step Detail is required').optional(),
  planId: z.number().int().positive('Plan is required').optional(),
  workDate: z.union([z.string(), z.date()]).refine((date) => {
    const d = new Date(date);
    const now = new Date();
    const threeMonthsAgo = new Date(now.getFullYear(), now.getMonth() - 3, now.getDate());
    return d >= threeMonthsAgo && d <= now;
  }, 'Work date must be within the last 3 months').optional(),
  entryDate: z.union([z.string(), z.date()]).optional(),
  actualQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  plannedQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  limitQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  previousEnteredQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  unitPrice: z.number().min(0, 'Value must be non-negative').max(99999999.99, 'Unit Price exceeds maximum allowed value').optional(),
  totalAmount: z.number().min(0, 'Value must be non-negative').max(9999999999.99, 'Total Amount exceeds maximum allowed value').optional(),
  salaryNote: z.string().trim().optional(),
  status: z.string().trim().regex(/^(draft|submitted|approved|paid|cancelled|)$/, 'Invalid status value').optional(),
  approvedBy: z.string().trim().optional(),
  approvedAt: z.union([z.string(), z.date()]).optional(),
  startTime: z.union([z.string(), z.date()]).refine((date) => {
    if (!date) {
      return true;
    }
    const d = new Date(date);
    const now = new Date();
    const oneDayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1000);
    return d >= oneDayAgo && d <= now;
  }, 'Start Time must be within the last 24 hours').optional(),
  endTime: z.union([z.string(), z.date()]).refine((date) => {
    if (!date) {
      return true;
    }
    const d = new Date(date);
    const now = new Date();
    const oneDayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1000);
    return d >= oneDayAgo && d <= now;
  }, 'End Time must be within the last 24 hours').optional(),
  workDurationMinutes: z.number().int().min(0).max(1440, 'Work duration cannot exceed 24 hours').optional(),
});

// 🆕 V4: Enhanced relation validation schemas

export const userIdSchema = z.object({
  userId: z.string().min(1, 'UserSync is required'),
});
export const productionStepDetailIdSchema = z.object({
  productionStepDetailId: z.number().int().positive('ProductionStepDetail is required'),
});
export const planIdSchema = z.object({
  planId: z.number().int().positive('Plan is required'),
});

// 🆕 V4: Enhanced list parameters validation with complex options
export const employeeSalaryEntryListParamsSchema = z.object({
  page: z.number().int().min(1).default(1),
  limit: z.number().int().min(1).max(100).default(10),
  search: z.string().trim().max(255).optional(),
  sortBy: z.enum(['createdAt', 'updatedAt', 'workDate', 'id', 'status']).default('createdAt'),
  sortOrder: z.enum(['asc', 'desc']).default('desc'),
  showAll: z.boolean().default(false),
  includeRelations: z.boolean().default(false),
  // 🆕 V4: Enhanced filtering options
  status: z.enum(['draft', 'submitted', 'approved', 'paid', 'cancelled']).optional(),

  dateFrom: z.union([z.string(), z.date()]).optional(),
  dateTo: z.union([z.string(), z.date()]).optional(),

  userId: z.string().optional(),
  productionStepDetailId: z.number().int().positive().optional(),
  planId: z.number().int().positive().optional(),
});

// 🆕 V4: Enhanced import row validation for Excel import with relations
export const importEmployeeSalaryEntryRowSchema = z.object({
  userId: z.string().min(1, 'Employee is required'),
  productionStepDetailId: z.number().int().positive('Production Step Detail is required'),
  planId: z.number().int().positive('Plan is required'),
  workDate: z.union([z.string(), z.date()]).refine((date) => {
    const d = new Date(date);
    const now = new Date();
    const threeMonthsAgo = new Date(now.getFullYear(), now.getMonth() - 3, now.getDate());
    return d >= threeMonthsAgo && d <= now;
  }, 'Work date must be within the last 3 months'),
  entryDate: z.union([z.string(), z.date()]).optional(),
  actualQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  plannedQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  limitQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  previousEnteredQuantity: z.number().int().min(0, 'Value must be non-negative').optional(),
  unitPrice: z.number().min(0, 'Value must be non-negative').max(99999999.99, 'Unit Price exceeds maximum allowed value').optional(),
  totalAmount: z.number().min(0, 'Value must be non-negative').max(9999999999.99, 'Total Amount exceeds maximum allowed value').optional(),
  salaryNote: z.string().trim().optional(),
  status: z.string().trim().regex(/^(draft|submitted|approved|paid|cancelled|)$/, 'Invalid status value').optional(),
  approvedBy: z.string().trim().optional(),
  approvedAt: z.union([z.string(), z.date()]).optional(),
  startTime: z.union([z.string(), z.date()]).refine((date) => {
    if (!date) {
      return true;
    }
    const d = new Date(date);
    const now = new Date();
    const oneDayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1000);
    return d >= oneDayAgo && d <= now;
  }, 'Start Time must be within the last 24 hours').optional(),
  endTime: z.union([z.string(), z.date()]).refine((date) => {
    if (!date) {
      return true;
    }
    const d = new Date(date);
    const now = new Date();
    const oneDayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1000);
    return d >= oneDayAgo && d <= now;
  }, 'End Time must be within the last 24 hours').optional(),
  workDurationMinutes: z.number().int().min(0).max(1440, 'Work duration cannot exceed 24 hours').optional(),
  rowNumber: z.number().int().positive(),
})
// Business logic validation for import
  .refine((data) => {
    if (data.actualQuantity && data.unitPrice) {
      data.totalAmount = data.actualQuantity * data.unitPrice;
    }
    return true;
  });

// ID validation
export const employeeSalaryEntryIdSchema = z.object({
  id: z.string().regex(/^\d+$/, 'ID must be a valid number').transform(Number),
});

// 🆕 V4: Batch operations validation
export const employeeSalaryEntryBatchSchema = z.object({
  ids: z.array(z.number().int().positive()).min(1, 'At least one item must be selected'),
  action: z.enum(['delete', 'approve', 'reject', 'updateStatus']),
  newStatus: z.enum(['draft', 'submitted', 'approved', 'paid', 'cancelled']).optional(),
});

// 🆕 V4: Enhanced validation helper functions
export function validateEmployeeSalaryEntryId(data: unknown) {
  return employeeSalaryEntryIdSchema.parse(data);
}

export function validateEmployeeSalaryEntryForm(data: unknown) {
  return employeeSalaryEntryFormSchema.parse(data);
}

export function validateCreateEmployeeSalaryEntry(data: unknown) {
  return createEmployeeSalaryEntrySchema.parse(data);
}

export function validateUpdateEmployeeSalaryEntry(data: unknown) {
  return updateEmployeeSalaryEntrySchema.parse(data);
}

export function validateEmployeeSalaryEntryListParams(data: unknown) {
  return employeeSalaryEntryListParamsSchema.parse(data);
}

export function validateEmployeeSalaryEntryBatch(data: unknown) {
  return employeeSalaryEntryBatchSchema.parse(data);
}

export function validateImportEmployeeSalaryEntryRow(data: unknown) {
  return importEmployeeSalaryEntryRowSchema.parse(data);
}
