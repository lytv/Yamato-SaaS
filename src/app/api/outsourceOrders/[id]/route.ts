/**
 * OutsourceOrder Detail API Routes with Relations Support
 * Generated by enhanced entity generator script V4 (improved)
 */

import { auth } from '@clerk/nextjs/server';
import type { NextRequest } from 'next/server';
import { NextResponse } from 'next/server';

import {
  deleteOutsourceOrder,
  getOutsourceOrderById,
  updateOutsourceOrder,
} from '@/libs/queries/outsourceOrder';
import {
  validateUpdateOutsourceOrder,
} from '@/libs/validations/outsourceOrder';

type RouteParams = {
  params: { id: string };
};

// GET /api/outsourceOrders/[id]
export async function GET(request: NextRequest, { params }: RouteParams) {
  try {
    const { userId } = await auth();
    if (!userId) {
      return NextResponse.json(
        { success: false, error: 'Unauthorized', code: 'AUTH_REQUIRED' },
        { status: 401 },
      );
    }

    const id = Number.parseInt(params.id);
    if (Number.isNaN(id)) {
      return NextResponse.json(
        { success: false, error: 'Invalid ID format', code: 'INVALID_ID' },
        { status: 400 },
      );
    }

    const { searchParams } = new URL(request.url);
    const includeRelations = searchParams.get('includeRelations') === 'true';

    const outsourceOrder = await getOutsourceOrderById(id, userId, includeRelations);

    if (!outsourceOrder) {
      return NextResponse.json(
        { success: false, error: 'OutsourceOrder not found', code: 'NOT_FOUND' },
        { status: 404 },
      );
    }

    return NextResponse.json({
      success: true,
      data: outsourceOrder,
    });
  } catch (error) {
    console.error(`GET /api/outsourceOrders/[id] error:`, error);
    return NextResponse.json(
      {
        success: false,
        error: error instanceof Error ? error.message : 'Internal server error',
        code: 'INTERNAL_ERROR',
      },
      { status: 500 },
    );
  }
}

// PUT /api/outsourceOrders/[id]
export async function PUT(request: NextRequest, { params }: RouteParams) {
  try {
    console.log('🐛 API PUT called with params:', params);
    
    const { userId } = await auth();
    if (!userId) {
      return NextResponse.json(
        { success: false, error: 'Unauthorized', code: 'AUTH_REQUIRED' },
        { status: 401 },
      );
    }

    const id = Number.parseInt(params.id);
    if (Number.isNaN(id)) {
      return NextResponse.json(
        { success: false, error: 'Invalid ID format', code: 'INVALID_ID' },
        { status: 400 },
      );
    }

    const body = await request.json();
    console.log('🐛 Request body:', JSON.stringify(body));

    // Check if body is valid object
    if (!body || typeof body !== 'object') {
      return NextResponse.json(
        {
          success: false,
          error: 'Invalid request body',
          code: 'VALIDATION_ERROR',
        },
        { status: 400 },
      );
    }

    const validation = validateUpdateOutsourceOrder(body);
    if (!validation.success) {
      console.log('🐛 Validation failed:', validation.error);
      return NextResponse.json(
        {
          success: false,
          error: 'Validation failed',
          code: 'VALIDATION_ERROR',
          details: validation.error.errors,
        },
        { status: 400 },
      );
    }

    console.log('🐛 About to call updateOutsourceOrder');
    const updatedOutsourceOrder = await updateOutsourceOrder(id, validation.data, userId);
    console.log('🐛 updateOutsourceOrder completed successfully');

    return NextResponse.json({
      success: true,
      data: updatedOutsourceOrder,
      message: 'OutsourceOrder updated successfully',
    });
  } catch (error) {
    console.error(`PUT /api/outsourceOrders/[id] error:`, error);

    if (error instanceof Error) {
      if (error.message.includes('not found')) {
        return NextResponse.json(
          { success: false, error: 'OutsourceOrder not found', code: 'NOT_FOUND' },
          { status: 404 },
        );
      }

      if (error.message.includes('access denied')) {
        return NextResponse.json(
          { success: false, error: 'Access denied', code: 'ACCESS_DENIED' },
          { status: 403 },
        );
      }
    }

    return NextResponse.json(
      {
        success: false,
        error: error instanceof Error ? error.message : 'Internal server error',
        code: 'INTERNAL_ERROR',
      },
      { status: 500 },
    );
  }
}

// DELETE /api/outsourceOrders/[id]
export async function DELETE(_request: NextRequest, { params }: RouteParams) {
  try {
    const { userId } = await auth();
    if (!userId) {
      return NextResponse.json(
        { success: false, error: 'Unauthorized', code: 'AUTH_REQUIRED' },
        { status: 401 },
      );
    }

    const id = Number.parseInt(params.id);
    if (Number.isNaN(id)) {
      return NextResponse.json(
        { success: false, error: 'Invalid ID format', code: 'INVALID_ID' },
        { status: 400 },
      );
    }

    const success = await deleteOutsourceOrder(id, userId);

    if (!success) {
      return NextResponse.json(
        { success: false, error: 'OutsourceOrder not found or access denied', code: 'NOT_FOUND' },
        { status: 404 },
      );
    }

    return NextResponse.json({
      success: true,
      message: 'OutsourceOrder deleted successfully',
    });
  } catch (error) {
    console.error(`DELETE /api/outsourceOrders/[id] error:`, error);
    return NextResponse.json(
      {
        success: false,
        error: error instanceof Error ? error.message : 'Internal server error',
        code: 'INTERNAL_ERROR',
      },
      { status: 500 },
    );
  }
}
